.DS_Store
node_modules/
.env
.env.*
!.env.example
.env.local
*.log
.mcp.json

# python cache
__pycache__/
*.pyc

# large datasets — keep refs in research/, not raw blobs
data/raw/
data/*.csv
data/*.parquet
data/*.zip
data/*.xlsx
!data/.gitkeep
# but DO commit small derived artifacts (e.g. LLM scoring results)
!data/llm_artifacts/

# editor swap files
*.swp
*.swo

# private team profiles — never commit
team.md
team/

# personal punch-lists — never commit
/requirements*.md
docs/PLAN-REMAINING*.md

# owner work folders — ignored until we pick a direction (each member proposes; we converge)
tero/
mian/
.superpowers/
