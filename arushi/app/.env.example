# Public Render/Fly URL of Tero's deployed FastAPI. Empty → fall back to mocks.
VITE_PUBLIC_URL=

# Demo auth (Tero Block 35c). Required for /book and /outcome only — /sse, /triage,
# /recommend, /health stay open. Empty → mutating endpoints return 401 in prod.
VITE_DEMO_KEY=
